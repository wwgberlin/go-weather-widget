{{define "layout"}}
	<html>
		{{ template "head" .}}
		<body>
			{{ template "content" .}}
			{{ template "scripts" .}}
		</body>
	</html>
{{ end }}

<!-- we can add some defaults -->
{{define "scripts"}}
{{end}}
{{define "head"}}
{{end}}
{{define "content"}}
{{end}}
