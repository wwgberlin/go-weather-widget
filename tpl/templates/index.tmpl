{{template "layout"}}

{{define "title"}}
	Weather Forecast
{{end}}

{{define "content"}}
	<h1>What's the weather in:</h1>
	<form action="/weather">
		<input type="text" name="location" placeholder="Location" value="{{ .query }}" required>
	</form>
{{end}}
